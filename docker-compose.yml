services:
  postgres:
    # GHCR owner values must be lowercase to match the publish workflow output.
    image: ghcr.io/${GHCR_OWNER:?set GHCR_OWNER}/postgres-for-ai:${GHCR_TAG:-main}
    container_name: ai
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: ai
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql
  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - postgres
volumes:
  pgdata:
